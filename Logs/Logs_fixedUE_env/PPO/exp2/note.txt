因為 exp1 有學起來的趨勢，但太慢了，這邊做以下的修正。
1. entropy_coef = 0.01 -> 0.005
2. batch_size = 64 > 32
3. epoch = 10 -> 20
4. ACTION_SACLE = 5 改為動態調整
    # [新增] Action Scale Scheduler (動態調整)
    # 策略：前 5000 步從 2.0 線性增加到 6.0，之後維持在 6.0
    # 為什麼起點是 2.0？因為 2.0 的 Softmax 峰值約 0.9，剛好夠用又不會梯度消失
    # 為什麼終點是 6.0？因為 6.0 的 Softmax 峰值約 0.99，足夠極端
    # -----------------------------------------------------------------------
    current_scale = min(6.0, 2.0 + (frame / 5000.0) * 4.0)
5. Model 使用 Orthogonal Init. 不然原本的 Kaiming Init. 太溫吞
環境一樣在 MDP 的 fixed UE 中跑。